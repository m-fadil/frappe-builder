name: Release

on:
  # Tag refs only: one GHCR version per released tag.
  push:
    tags: ["v*.*.*"]
  workflow_dispatch:
    inputs:
      cache_bust:
        description: "Force re-pull of all apps: enter any new value (e.g. today's date)"
        default: ""

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

# CACHE_BUST build-arg, manual half. Precedence: dispatch input > vars.CACHE_BUST.
# Concatenated per build step with steps.rev.outputs.frappe / .apps, which fold
# in resolved upstream commit SHAs so a moved branch busts on its own even
# without a manual CACHE_BUST bump. Kept as two separate hashes so an app
# update doesn't force a Frappe framework re-clone, and vice versa.
env:
  CACHE_BUST: ${{ github.event.inputs.cache_bust || vars.CACHE_BUST }}

jobs:
  verify:
    name: Verify Build
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Prepare apps.json and netrc with secrets
        env:
          APPS_JSON_CONTENT: ${{ secrets.APPS_JSON }}
          # Full .netrc content — see example.netrc for the expected format
          NETRC_CONTENT: ${{ secrets.NETRC }}
          SECRETS_CONTEXT: ${{ toJson(secrets) }}
        run: |
          if [ -n "$APPS_JSON_CONTENT" ]; then
            echo "$APPS_JSON_CONTENT" > /tmp/apps.json
          elif [ -f apps.json ]; then
            eval "$(jq -r 'to_entries[] | select(.key | IN("APPS_JSON","NETRC","github_token") | not) | "export \(.key)=\(.value|@sh)"' <<<"$SECRETS_CONTEXT")"
            envsubst < apps.json > /tmp/apps.json
          else
            echo "[]" > /tmp/apps.json
          fi

          # Git's HTTPS transport reads ~/.netrc automatically; installed on
          # the runner too so the host-side `git ls-remote` below (and not
          # just the container build) can authenticate to private hosts.
          printf '%s\n' "$NETRC_CONTENT" > /tmp/netrc
          chmod 600 /tmp/netrc
          cp /tmp/netrc "$HOME/.netrc"
          chmod 600 "$HOME/.netrc"

      # Git refs are not part of any layer cache key, and secret mounts are excluded
      # from cache keys by design, so neither a tag push nor an edited apps.json
      # invalidates the `framework`/`apps` stage RUNs. Fold resolved upstream SHAs
      # into their respective CACHE_BUST args instead, kept separate so an app
      # commit doesn't bust the Frappe framework clone. URLs may carry PATs: emit
      # the digest only, never a resolved URL.
      - name: Resolve upstream app revisions
        id: rev
        run: |
          set -eo pipefail
          FRAPPE_PATH=$(grep -oP '^ARG FRAPPE_PATH=\K.*' Containerfile)
          FRAPPE_BRANCH=$(grep -oP '^ARG FRAPPE_BRANCH=\K.*' Containerfile)
          frappe_sha=$(git ls-remote "$FRAPPE_PATH" "$FRAPPE_BRANCH" | cut -f1)
          [ -n "$frappe_sha" ] || { echo "::error::cannot resolve $FRAPPE_BRANCH in frappe"; exit 1; }
          echo "frappe=$(printf '%s\n' "$frappe_sha" | sha256sum | cut -c1-16)" >> "$GITHUB_OUTPUT"

          app_revs=""
          while IFS=$'\t' read -r url branch; do
            sha=$(git ls-remote "$url" "$branch" | cut -f1)
            [ -n "$sha" ] || { echo "::error::cannot resolve branch '$branch' for an apps.json entry"; exit 1; }
            app_revs="$app_revs $sha"
          done < <(jq -r '.[] | "\(.url)\t\(.branch)"' /tmp/apps.json)
          echo "apps=$(printf '%s\n' $app_revs | sort | sha256sum | cut -c1-16)" >> "$GITHUB_OUTPUT"

      - name: Build smoke-test image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Containerfile
          load: true
          tags: custom-frappe:verify
          build-args: |
            FRAPPE_CACHE_BUST=${{ env.CACHE_BUST }}-${{ steps.rev.outputs.frappe }}
            APPS_CACHE_BUST=${{ env.CACHE_BUST }}-${{ steps.rev.outputs.apps }}
          secrets: |
            id=apps_json,src=/tmp/apps.json
            id=netrc,src=/tmp/netrc
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test image
        run: |
          docker run --rm --entrypoint bash custom-frappe:verify -lc \
            "test -d /home/frappe/frappe-bench/apps/frappe && test -f /home/frappe/frappe-bench/assets/assets.json && bench --version"

  publish:
    name: Publish
    runs-on: ubuntu-latest
    needs: verify
    timeout-minutes: 60
    permissions:
      contents: read
      packages: write
      # Required by attest-build-provenance to sign the pushed digest.
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4

      - name: Prepare apps.json and netrc with secrets
        env:
          APPS_JSON_CONTENT: ${{ secrets.APPS_JSON }}
          # Full .netrc content — see example.netrc for the expected format
          NETRC_CONTENT: ${{ secrets.NETRC }}
          SECRETS_CONTEXT: ${{ toJson(secrets) }}
        run: |
          if [ -n "$APPS_JSON_CONTENT" ]; then
            echo "$APPS_JSON_CONTENT" > /tmp/apps.json
          elif [ -f apps.json ]; then
            eval "$(jq -r 'to_entries[] | select(.key | IN("APPS_JSON","NETRC","github_token") | not) | "export \(.key)=\(.value|@sh)"' <<<"$SECRETS_CONTEXT")"
            envsubst < apps.json > /tmp/apps.json
          else
            echo "[]" > /tmp/apps.json
          fi

          # Git's HTTPS transport reads ~/.netrc automatically; installed on
          # the runner too so the host-side `git ls-remote` below (and not
          # just the container build) can authenticate to private hosts.
          printf '%s\n' "$NETRC_CONTENT" > /tmp/netrc
          chmod 600 /tmp/netrc
          cp /tmp/netrc "$HOME/.netrc"
          chmod 600 "$HOME/.netrc"

      # Git refs are not part of any layer cache key, and secret mounts are excluded
      # from cache keys by design, so neither a tag push nor an edited apps.json
      # invalidates the `framework`/`apps` stage RUNs. Fold resolved upstream SHAs
      # into their respective CACHE_BUST args instead, kept separate so an app
      # commit doesn't bust the Frappe framework clone. URLs may carry PATs: emit
      # the digest only, never a resolved URL.
      - name: Resolve upstream app revisions
        id: rev
        run: |
          set -eo pipefail
          FRAPPE_PATH=$(grep -oP '^ARG FRAPPE_PATH=\K.*' Containerfile)
          FRAPPE_BRANCH=$(grep -oP '^ARG FRAPPE_BRANCH=\K.*' Containerfile)
          frappe_sha=$(git ls-remote "$FRAPPE_PATH" "$FRAPPE_BRANCH" | cut -f1)
          [ -n "$frappe_sha" ] || { echo "::error::cannot resolve $FRAPPE_BRANCH in frappe"; exit 1; }
          echo "frappe=$(printf '%s\n' "$frappe_sha" | sha256sum | cut -c1-16)" >> "$GITHUB_OUTPUT"

          app_revs=""
          while IFS=$'\t' read -r url branch; do
            sha=$(git ls-remote "$url" "$branch" | cut -f1)
            [ -n "$sha" ] || { echo "::error::cannot resolve branch '$branch' for an apps.json entry"; exit 1; }
            app_revs="$app_revs $sha"
          done < <(jq -r '.[] | "\(.url)\t\(.branch)"' /tmp/apps.json)
          echo "apps=$(printf '%s\n' $app_revs | sort | sha256sum | cut -c1-16)" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # v1.2.3 -> :1.2.3, :1.2, :1, plus :latest
      - name: Derive tags
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
          labels: |
            org.opencontainers.image.title=custom-frappe-docker
            org.opencontainers.image.description=Production Frappe custom image with bundled apps and zero-downtime asset management

      - name: Build and push
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Containerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          annotations: ${{ steps.meta.outputs.annotations }}
          build-args: |
            FRAPPE_CACHE_BUST=${{ env.CACHE_BUST }}-${{ steps.rev.outputs.frappe }}
            APPS_CACHE_BUST=${{ env.CACHE_BUST }}-${{ steps.rev.outputs.apps }}
          secrets: |
            id=apps_json,src=/tmp/apps.json
            id=netrc,src=/tmp/netrc
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true

      - name: Attest provenance
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ghcr.io/${{ github.repository }}
          subject-digest: ${{ steps.build.outputs.digest }}

      - name: Summary
        run: |
          {
            echo "### Published"
            echo
            echo '```'
            echo "${{ steps.meta.outputs.tags }}"
            echo '```'
            echo
            echo "Digest: \`${{ steps.build.outputs.digest }}\`"
          } >> "$GITHUB_STEP_SUMMARY"
